import { ReactElement } from "react";
import { AlertModalButtonProps } from "./types";
export declare const AlertModalButton: {
    (props: AlertModalButtonProps): ReactElement;
    displayName: string;
};
