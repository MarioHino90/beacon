import { ReactElement } from "react";
import { AlertModalProps } from "./types";
export declare const AlertModal: {
    ({ children, className, intent, size, ...props }: AlertModalProps): ReactElement;
    displayName: string;
};
