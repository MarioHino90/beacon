import React from "react";
import { NumberInputButtonProps } from "../types";
export declare const NumberInputButton: {
    ({ className, direction, ...props }: NumberInputButtonProps): React.ReactElement | null;
    displayName: string;
};
