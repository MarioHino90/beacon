import React from "react";
import { CollapsibleButtonProps } from "./types";
export declare const CollapsibleButton: {
    ({ children, className, variant, ...props }: CollapsibleButtonProps): React.ReactElement;
    displayName: string;
};
