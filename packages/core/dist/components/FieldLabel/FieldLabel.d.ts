import React from "react";
import { FieldLabelProps } from "./types";
export declare const FieldLabel: ({ children, className, enforce, formatLabel, helpText, label, status, statusText, boxProps, infoIconVariant, infoIconPopoverContent, infoIconTriggerVariant, controlRef, ...props }: FieldLabelProps) => React.ReactElement;
