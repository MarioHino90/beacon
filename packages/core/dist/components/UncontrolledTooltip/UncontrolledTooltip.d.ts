import React from "react";
import { UncontrolledTooltipProps } from "./types";
export declare const UncontrolledTooltip: ({ children, targetRef, className, label, ...props }: UncontrolledTooltipProps) => React.ReactElement;
