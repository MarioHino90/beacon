import React from "react";
import { InlineInputEditModeProps } from "../types";
export declare const InlineInputEditMode: {
    ({ cancelButtonRef, className, inputId, inputRef, invalid, onChange, onSaveClick, onCancelClick, onClearClick, saveButtonRef, value, vSize, ...props }: InlineInputEditModeProps): React.ReactElement | null;
    displayName: string;
};
