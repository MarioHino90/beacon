import React from "react";
import { InlineInputViewModeProps } from "../types";
export declare const InlineInputViewMode: {
    ({ children, className, placeholder, value, ...props }: InlineInputViewModeProps): React.ReactElement;
    displayName: string;
};
