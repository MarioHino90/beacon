import React from "react";
import { AddDocumentProps } from "../types";
export declare const AddDocument: {
    ({ className, ...props }: AddDocumentProps): React.ReactElement;
    displayName: string;
};
