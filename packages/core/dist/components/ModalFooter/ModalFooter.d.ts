import React from "react";
import { ModalFooterProps } from "./types";
export declare const ModalFooter: {
    ({ className, ...props }: ModalFooterProps): React.ReactElement;
    displayName: string;
};
