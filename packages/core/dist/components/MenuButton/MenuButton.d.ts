import React from "react";
import { MenuButtonProps } from "./types";
export declare const MenuButton: {
    ({ className, ...props }: MenuButtonProps): React.ReactElement;
    displayName: string;
};
