import React from "react";
import { MenuActionButtonProps } from "./types";
export declare const MenuActionButton: {
    ({ children, className, iconName, ...props }: MenuActionButtonProps): React.ReactElement;
    displayName: string;
};
