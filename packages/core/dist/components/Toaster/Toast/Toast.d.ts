import React from "react";
import { ToastProps } from "../types";
export declare const Toast: {
    ({ children, className, intent, ...props }: ToastProps): React.ReactElement | null;
    displayName: string;
};
