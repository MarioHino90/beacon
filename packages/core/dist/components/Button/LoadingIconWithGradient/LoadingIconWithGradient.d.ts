import React from "react";
import { LoadingIconWithGradientProps } from "../types";
export declare const LoadingIconWithGradient: ({ intent, size, variant, }: LoadingIconWithGradientProps) => React.ReactElement;
