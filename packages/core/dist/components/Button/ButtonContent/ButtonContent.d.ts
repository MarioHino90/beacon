import React from "react";
import { ButtonContentProps } from "../types";
export declare const ButtonContent: ({ children, loadingMessage, loadingState, intent, size, variant, }: ButtonContentProps) => React.ReactElement;
