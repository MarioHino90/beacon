import React from "react";
import { CollapsibleProps } from "./types";
export declare const Collapsible: {
    ({ className, isOpen, toggle, alignment, ...props }: CollapsibleProps): React.ReactElement;
    displayName: string;
};
