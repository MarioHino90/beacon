import React from "react";
import { StatusTextProps } from "./types";
export declare const StatusText: ({ className, children, displayIcon, status, ...props }: StatusTextProps) => React.ReactElement;
