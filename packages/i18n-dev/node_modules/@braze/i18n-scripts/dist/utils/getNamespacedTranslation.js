"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getNamespacedTranslation = void 0;
/**
 * Convert a dotted.key => value into { dotted: { key: value } }
 * @param {string} key
 * @param {string} value
 * @returns {import("../i18n").NamespacedKeyTranslation}
 */
function getNamespacedTranslation(key, value) {
    const keys = key.split(".");
    if (keys.length > 2) {
        throw new Error(`translation key "${key}" is too deeply nested. Only namespace plus message key is allowed`);
    }
    if (keys.length < 2) {
        throw new Error(`translation key "${key}" must have a namespace. Only namespaces message keys are allowed`);
    }
    return {
        [keys[0]]: {
            [keys[1]]: value,
        },
    };
}
exports.getNamespacedTranslation = getNamespacedTranslation;
//# sourceMappingURL=getNamespacedTranslation.js.map