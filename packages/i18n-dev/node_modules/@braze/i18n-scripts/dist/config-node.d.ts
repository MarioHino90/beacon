import convict from "convict";
import type { I18nConfig } from "./i18n";
export declare const validatePath: (configPath: string) => (type: "path" | "file") => (sources: unknown) => void;
export declare function loadConfig(configPath: string | I18nConfig): convict.Config<I18nConfig>;
/**
 * Save a new I18n configuration for use in future commands
 */
export declare function saveConfig(values: I18nConfig, configPath?: string): void;
