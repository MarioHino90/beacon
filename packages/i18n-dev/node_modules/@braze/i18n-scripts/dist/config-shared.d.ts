/**
 * @fileoverview shared functionality between node and the browser. This sets up the validations with custom format validation
 * in Node, the validators check that paths are valid. In the browser, it just verifies that they are strings
 */
import type Convict from "convict";
import type { Config } from "convict";
import type { I18nConfig } from "./i18n";
export declare type Validate = (type: "file" | "path") => (source: unknown) => void;
export declare function getI18nConfig(convict: Convict, validatePath: Validate): Config<I18nConfig>;
