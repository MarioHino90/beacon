/**
 * @fileoverview webpack-compatible version of config
 * this ignores paths, which are useless in the frontend anyways
 * it also only loads from a JSON object instead of a path
 */
import type Convict from "convict";
import type { I18nConfig } from "./i18n";
export declare const validatePath: (type: "path" | "file") => (sources: unknown) => void;
export declare function loadConfig(config: string | I18nConfig): Convict.Config<I18nConfig>;
/**
 * This is a no-op in the browser
 */
export declare function saveConfig(): void;
