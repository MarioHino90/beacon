"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createMockPseudoEnv = createMockPseudoEnv;
var translations = {
  en: {
    braze: {
      someNamespace: {
        someKey: "someTranslation"
      }
    }
  }
};

function createMockPseudoEnv(processOverrides) {
  return {
    defaultNS: "braze",
    fallbackLng: "en",
    // enable post-processing for full pseudo-localization support only if the env variable is set
    // AND we are in development
    postProcess: processOverrides.NODE_ENV === "development" && processOverrides.I18N_BACKEND === "pseudolocalization" ? "pseudo" : undefined,
    detection: {
      order: ["querystring", "cookie"],
      lookupQuerystring: "locale"
    },
    resources: translations,
    supportedLngs: ["en"]
  };
}