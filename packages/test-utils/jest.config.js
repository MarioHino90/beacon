// eslint-disable-next-line @typescript-eslint/no-var-requires
const baseConfig = require("../../jest.config");

// TODO: bring coverage to 95% and drop custom overrides
module.exports = {
  ...baseConfig,
};
